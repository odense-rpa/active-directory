[project]
name = "active-directory"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ldap3>=2.9.1",
    "pytest>=8.4.1"
]

[tool.uv.sources]

